import React from 'react';
import {Text, View} from 'react-native';
import {styles} from '../theme/apptheme';

export const personaScreen = () => {
  return (
    <View style={styles.globalMargin}>
      <Text style={styles.tittle}>Persona Screen</Text>
    </View>
  );
};
